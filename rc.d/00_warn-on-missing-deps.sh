#!/usr/bin/env bash


# check for these to be installed (needed for installing mise postgres), and print how to install if not
# macos
# xcode-select --install
# brew install openssl readline
# deb/ubuntu
# sudo apt-get install build-essential libreadline-dev zlib1g-dev libssl-dev uuid-dev bison
