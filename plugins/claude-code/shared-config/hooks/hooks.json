{
  "description": "shared-config hooks: Setup + SessionStart sync-shared-config (clone shared-config source repos, symlink rules/skills/commands/agents into the project via a deduped shared cache, optionally merge settings). Prefers the github-app token; waits briefly for it to appear in CLAUDE_ENV_FILE.",
  "hooks": {
    "Setup": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync-shared-config.sh",
            "timeout": 120
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/sync-shared-config.sh",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
