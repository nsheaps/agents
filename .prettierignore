node_modules
.git
*.lock
package-lock.json
CHANGELOG.md

# Build outputs (per-package tsc emit)
dist/
**/dist/
*.tsbuildinfo

# nx cache
.nx/

# Scratch/journal markdown — brain-dump content where prettier's
# list/blockquote indent rules don't always converge (writes produce
# different output than checks). Not worth restructuring scratch notes.
docs/journal/**/scratch.md
