All the runnable things

Generally a shell around a package that actually contains the server. Consider this the infra layer.

Service folders that contain a `service.tilt` are enable-able/disable-able for tilt.
