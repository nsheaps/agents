# PR Review: op-exec#2 — P1 Consistency Pass

**Reviewer**: Daffy D (qa)
**Date**: 2026-02-23
**PR**: nsheaps/op-exec#2
**Type**: Consistency pass (check.yaml, .shellcheckrc, .editorconfig, shfmt, mise tasks)
**Reference**: dotfiles#8 (merged baseline)

---

## Summary

**Overall Score: 90/100 ✅ — Ready to merge**

Corrected from sub-agent's 84/100 after verifying findings against dotfiles#8 and the other consistency PRs. Both P2 findings were false positives — the patterns match the established baseline.

## Category Scores

| # | Category | Score | Status |
|---|----------|-------|--------|
| 1 | Simplicity | 90 | ✅ |
| 2 | Flexibility | 88 | ✅ |
| 3 | Usability | 88 | ✅ |
| 4 | Documentation | 85 | ✅ |
| 5 | Security | 90 | ✅ |
| 6 | Pattern Matching | 92 | ✅ |
| 7 | Best Practices | 88 | ✅ |
| 8 | General QA | 90 | ✅ |

---

## Findings

### ci-1 (P3): Duplicate editorconfig-checker runs

**File**: `.github/workflows/check.yaml:82-85`
**Severity**: P3
**Description**: The workflow uses `editorconfig-checker/action-editorconfig-checker@main` (which installs AND runs the checker), then immediately runs `editorconfig-checker` again manually. The check executes twice.
**Note**: This is systematic — all three consistency PRs (claude-utils#8, git-wt#11, op-exec#2) have the same pattern. Track as a cross-repo follow-up.

### ci-2 (P3): No explicit permissions block

**File**: `.github/workflows/check.yaml`
**Severity**: P3
**Description**: No `permissions:` block declared. Consistent with all other repos (dotfiles, claude-utils, git-wt). Track as cross-repo follow-up.

### ~~flex-1 (was P2)~~: Find hardcodes bin/ — FALSE POSITIVE

**DISMISSED**: Same `find ./bin -maxdepth 1` pattern used in dotfiles#8 and claude-utils#8. For a single-script repo, this is appropriate. The `-not -name '*.sh'` filter exists because extensionless scripts are found by shebang detection. Consistent with established pattern.

### ~~flex-2 (was P2)~~: mise tasks hardcode bin/op-exec — FALSE POSITIVE

**DISMISSED**: git-wt#11 also uses explicit file lists (`bin/git-wt test/cli-test.sh`). For single-script repos, explicit names are simpler (KISS) than dynamic discovery. The old loop approach (`for script in bin/*`) only did `bash -n` (syntax check), not shellcheck — so the replacement is a net improvement regardless.

### ~~pattern-1 (was P2)~~: Push-only trigger — FALSE POSITIVE

**DISMISSED**: Dotfiles#8 also uses `push:` only — verified via GitHub API. Consistent pattern across all repos.

### ~~pattern-2 (was P3)~~: shfmt not installed — FALSE POSITIVE

**DISMISSED**: dotfiles#8 and claude-utils#8 also omit shfmt installation and both merged with CI passing. ubuntu-latest includes shfmt. Consistent pattern.

---

## Verified Correct

- **Concurrency group**: `github.ref`/`github.sha` — no `pull_request.number` (dotfiles#8 P1 avoided)
- **shfmt**: `shfmt -d $bash_files` with `.editorconfig` source of truth — no hardcoded flags (dotfiles#8 P2 avoided)
- **Shell changes**: All `bin/op-exec` changes are whitespace-only (case indent, pipe continuation, heredoc spacing). Zero logic changes.
- **`.editorconfig`**: Has `[*.sh]` with `switch_case_indent = true`
- **`.shellcheckrc`**: Disables SC1091 with justification. Minimal and appropriate. `shell=bash` correct.
- **mise tasks**: lint, fmt, fmt-check, check (with depends). Clean structure.
- **No secrets or hardcoded paths**

---

## Pattern Match Table

| Element | Expected (dotfiles#8) | Actual (op-exec#2) | Match |
|---------|----------------------|---------------------|-------|
| `.editorconfig` with `[*.sh]` | Yes | Yes | PASS |
| `.shellcheckrc` with directives | Yes | Yes | PASS |
| `check.yaml` with shellcheck + shfmt | Yes | Yes | PASS |
| Concurrency group (ref/sha) | Yes | Yes | PASS |
| Push-only trigger | Yes | Yes | PASS |
| shfmt from `.editorconfig` | Yes | Yes | PASS |
| mise tasks | Yes | Yes | PASS |
| Formatting-only changes | Yes | Yes | PASS |
| No permissions block | Absent | Absent | PASS |

---

*Report generated by Daffy D (qa) on 2026-02-23. Corrected from sub-agent initial score of 84% after independent verification against dotfiles#8 baseline.*
