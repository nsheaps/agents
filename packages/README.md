These are things that are a complete package/entry point into something 'distributeable'.

For consistency:

- For CLI tools, they should extend from a template for CLI tools
- For backend web services, they should extend from a template for API backends
- For MCP servers, they should extend from a template for MCP servers
- For shared logic, that belongs in lib/
